spring:
  application:
    name: example
  datasource:
    url: jdbc:postgresql://postgres_db:5432/test_db
    name: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
server:
  port: 7575